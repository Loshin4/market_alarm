plugins {
    id 'com.android.application'
}

android {
    namespace 'com.marketalarm.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.marketalarm.app'
        minSdk 26
        targetSdk 35
        versionCode 5
        versionName '0.5.0'
    }

    buildTypes {
        release { minifyEnabled false }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

// This app uses only Android platform APIs.
// Keeping this block empty avoids Kotlin runtime version conflicts.
dependencies {
}
